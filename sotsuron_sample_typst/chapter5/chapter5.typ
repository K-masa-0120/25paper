= 実験結果と考察
== 実験1
=== 結果
実験1の環境における正規化入力の結果を@fig:51 と@tbl:5-1 、モジュロ演算入力の結果を@fig:52 と@tbl:5-2、インデックス指定値入力の結果を@fig:53 と@tbl:5-3、従来のDQNモデルの結果を@fig:54 と@tbl:5-4 に示す。

表に示す数値のうち、総移動時間は単位時間の累計であり、経路一致はエピソード数であり、完全一致と部分一致は重複した累計である。
#figure(
  caption:[実験1、正規化入力のグラフ],
  image("../img/page5-1Nomalize .png",height: 470pt)
)<51>

#figure(
  caption:[実験1、モジュロ演算入力のグラフ],
  image("../img/page5-2mono.png")
)<52>

#figure(
  caption:[実験1、インデックス指定値入力のグラフ],
  image("../img/page5-3index.png")
)<53>

#figure(
  caption:[実験1、従来DQNのグラフ],
  image("../img/page5-4.png")
)<54>

#figure(
  caption: [実験1、正規化入力の結果],
  table(
    columns: 2,
    table.header([計測種類],[カウント]),
    [総移動時間], [92052],
    [完全一致＃1], [1594],
    [完全一致＃2], [1617],
    [完全一致＃3], [1492],
    [部分一致＃1], [1713],
    [部分一致＃2], [1728],
    [部分一致＃3], [1594],
    )
)<5-1>
#figure(
  caption: [実験1、モジュロ演算入力の結果],
  table(
    columns: 2,
    table.header([計測種類],[カウント]),
    [総移動時間], [91796],
    [完全一致＃1], [1590],
    [完全一致＃2], [1607],
    [完全一致＃3], [1513],
    [部分一致＃1], [1715],
    [部分一致＃2], [1706],
    [部分一致＃3], [1621],
    )   
)<5-2>
#figure(
  caption: [実験1、インデックス指定値入力の結果],
  table(
    columns: 2,
    table.header([計測種類],[カウント]),
    [総移動時間], [93427],
    [完全一致＃1], [1660],
    [完全一致＃2], [1982],
    [完全一致＃3], [1057],
    [部分一致＃1], [1783],
    [部分一致＃2], [2121],
    [部分一致＃3], [1127],
    )
)<5-3>
#figure(
  caption: [実験1、従来DQNの結果],
  table(
    columns: 2,
    table.header([計測種類],[カウント]),
    [総移動時間], [124765],
    [完全一致＃1], [1242],
    [完全一致＃2], [1359],
    [完全一致＃3], [1231],
    [部分一致＃1], [1701],
    [部分一致＃2], [1826],
    [部分一致＃3], [1671],
    )
)<5-4>

=== 考察

== 実験2
=== 結果
実験2の環境における正規化入力の結果を@fig:55 と@tbl:5-5、モジュロ演算入力の結果を@fig:56 と@tbl:5-6、インデックス指定値入力の結果を@fig:57 と@tbl:5-7、従来のDQNモデルの結果を@fig:58 と@tbl:5-8 に示す。
#figure(
  caption:[実験2、正規化入力のグラフ],
  image("../img/page5-5.png",height: 470pt)
)<55>

#figure(
  caption:[実験2、モジュロ演算入力のグラフ],
  image("../img/page5-6.png")
)<56>

#figure(
  caption:[実験2、インデックス指定値入力のグラフ],
  image("../img/page5-7.png")
)<57>

#figure(
  caption:[実験2、従来DQNのグラフ],
  image("../img/page5-8.png")
)<58>


#figure(
  caption: [実験2、正規化入力の結果],
  table(
    columns: 2,
    table.header([計測種類],[カウント]),
    [総移動時間], [524173],
    [完全一致＃1], [1615],
    [完全一致＃2], [2550],
    [完全一致＃3], [1019],
    [部分一致＃1], [3366],
    [部分一致＃2], [4453],
    [部分一致＃3], [2462],
    )
)<5-5>

#figure(
  caption: [実験2、モジュロ演算入力の結果],
  table(
    columns: 2,
    table.header([計測種類],[カウント]),
    [総移動時間], [492539],
    [完全一致＃1], [1760],
    [完全一致＃2], [2757],
    [完全一致＃3], [772],
    [部分一致＃1], [3498],
    [部分一致＃2], [4642],
    [部分一致＃3], [2096],
    )
)<5-6>

#figure(
  caption: [実験2、インデックス指定値入力の結果],
  table(
    columns: 2,
    table.header([計測種類],[カウント]),
    [総移動時間], [669609],
    [完全一致＃1], [558],
    [完全一致＃2], [2511],
    [完全一致＃3], [1960],
    [部分一致＃1], [1895],
    [部分一致＃2], [4711],
    [部分一致＃3], [3638],
    )
)<5-7>

#figure(
  caption: [実験2、従来DQNの結果],
  table(
    columns: 2,
    table.header([計測種類],[カウント]),
    [総移動時間], [5719407],
    [完全一致＃1], [13],
    [完全一致＃2], [0],
    [完全一致＃3], [0],
    [部分一致＃1], [5187],
    [部分一致＃2], [652],
    [部分一致＃3], [330],
    )
)<5-8>

=== 考察


#pagebreak()